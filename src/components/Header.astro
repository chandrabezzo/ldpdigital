---
const pathname = new URL(Astro.request.url).pathname;
const currentPath = pathname.slice(1);
---

<script>
  import { getCurrentLanguage, getTranslation } from '../utils/i18n';
</script>
<header class="header">
  <nav class="nav">
    <div class="container">
      <div class="nav-content">
        <div class="logo">
          <a href="/" class="logo-link">
            <div class="logo-icon">
              <svg viewBox="0 0 40 40" fill="none" xmlns="http://www.w3.org/2000/svg">
                <rect width="40" height="40" rx="8" fill="url(#logoGradient)"/>
                <path d="M12 12h16v4H12v-4zM12 18h16v4H12v-4zM12 24h12v4H12v-4z" fill="white"/>
                <defs>
                  <linearGradient id="logoGradient" x1="0" y1="0" x2="40" y2="40">
                    <stop stop-color="#0066CC"/>
                    <stop offset="1" stop-color="#003D7A"/>
                  </linearGradient>
                </defs>
              </svg>
            </div>
            <div class="logo-text">
              <div class="brand-name">LDP Digital</div>
              <div class="tagline">Software Solutions</div>
            </div>
          </a>
        </div>

        <div class="nav-links">
          <a href="/" class={currentPath === '' ? 'nav-link active' : 'nav-link'} data-i18n="nav.home">Beranda</a>
          <a href="/about" class={currentPath === 'about' ? 'nav-link active' : 'nav-link'} data-i18n="nav.about">Tentang Kami</a>
          <a href="/services" class={currentPath === 'services' ? 'nav-link active' : 'nav-link'} data-i18n="nav.services">Layanan</a>
          <a href="/portfolio" class={currentPath === 'portfolio' ? 'nav-link active' : 'nav-link'} data-i18n="nav.portfolio">Portfolio</a>
          <a href="/contact" class={currentPath === 'contact' ? 'nav-link active' : 'nav-link'} data-i18n="nav.contact">Kontak</a>
        </div>

        <div class="nav-actions">
          <div class="nav-lang">
            <div class="language-switcher">
              <button class="lang-btn" id="navLangBtn">
                <svg class="lang-icon" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2">
                  <circle cx="12" cy="12" r="10"/>
                  <path d="M2 12h20"/>
                  <path d="M12 2a15.3 15.3 0 0 1 4 10 15.3 15.3 0 0 1-4 10 15.3 15.3 0 0 1-4-10 15.3 15.3 0 0 1 4-10z"/>
                </svg>
                <span id="navCurrentLang">ID</span>
              </button>
              
              <div class="lang-dropdown" id="navLangDropdown">
                <button class="lang-option" data-lang="id">
                  <span class="flag">🇮🇩</span>
                  <span>Bahasa Indonesia</span>
                </button>
                <button class="lang-option" data-lang="en">
                  <span class="flag">🇺🇸</span>
                  <span>English</span>
                </button>
              </div>
            </div>
          </div>
          <a href="/contact" class="btn btn-primary" data-i18n="nav.consultation">Konsultasi Gratis</a>
        </div>

        <!-- Mobile Language Switcher - moved to header area -->
        <div class="mobile-lang-header" id="mobileLangHeader">
          <div class="language-switcher">
            <button class="lang-btn mobile-lang-btn-header" id="mobileLangBtnHeader">
              <div class="lang-btn-content">
                <svg class="lang-icon" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2">
                  <circle cx="12" cy="12" r="10"/>
                  <path d="M2 12h20"/>
                  <path d="M12 2a15.3 15.3 0 0 1 4 10 15.3 15.3 0 0 1-4 10 15.3 15.3 0 0 1-4-10 15.3 15.3 0 0 1 4-10z"/>
                </svg>
                <span id="mobileCurrentLangHeader">ID</span>
              </div>
              <svg class="chevron" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2">
                <polyline points="6,9 12,15 18,9"/>
              </svg>
            </button>
            
            <div class="lang-dropdown mobile-lang-dropdown-header" id="mobileLangDropdownHeader">
              <button class="lang-option" data-lang="id">
                <span class="flag">🇮🇩</span>
                <span>Bahasa Indonesia</span>
              </button>
              <button class="lang-option" data-lang="en">
                <span class="flag">🇺🇸</span>
                <span>English</span>
              </button>
            </div>
          </div>
        </div>

        <button class="mobile-menu-btn" id="mobileMenuBtn">
          <span></span>
          <span></span>
          <span></span>
        </button>
      </div>
    </div>
  </nav>

  <div class="mobile-menu" id="mobileMenu">
    <div class="mobile-menu-content">
      <a href="/" class="mobile-nav-link" data-i18n="nav.home">Beranda</a>
      <a href="/about" class="mobile-nav-link" data-i18n="nav.about">Tentang Kami</a>
      <a href="/services" class="mobile-nav-link" data-i18n="nav.services">Layanan</a>
      <a href="/portfolio" class="mobile-nav-link" data-i18n="nav.portfolio">Portfolio</a>
      <a href="/contact" class="mobile-nav-link" data-i18n="nav.contact">Kontak</a
      
      <div class="mobile-actions">
        <a href="/contact" class="btn btn-primary" data-i18n="nav.consultation">Konsultasi Gratis</a>
      </div>
    </div>
  </div>
</header>

<style>
  .header {
    position: fixed;
    top: 0;
    left: 0;
    right: 0;
    z-index: 1000;
    background: rgba(255, 255, 255, 0.95);
    backdrop-filter: blur(10px);
    border-bottom: 1px solid var(--neutral-200);
    transition: var(--transition);
  }

  .nav {
    padding: var(--spacing-sm) 0;
  }

  .nav-content {
    display: flex;
    align-items: center;
    justify-content: space-between;
    gap: var(--spacing-lg);
  }

  .logo-link {
    display: flex;
    align-items: center;
    gap: var(--spacing-xs);
    text-decoration: none;
    transition: var(--transition);
  }

  .logo-link:hover {
    transform: scale(1.05);
  }

  .logo-img {
    width: 90px;
    height: 35px;
  }

  .logo-text {
    display: flex;
    flex-direction: column;
  }

  .brand-name {
    font-size: 1.25rem;
    font-weight: 700;
    color: var(--neutral-800);
    line-height: 1;
  }

  .tagline {
    font-size: 0.75rem;
    color: var(--primary-blue);
    font-weight: 500;
  }

  .nav-links {
    display: flex;
    align-items: center;
    gap: var(--spacing-lg);
  }

  .nav-link {
    text-decoration: none;
    color: var(--neutral-600);
    font-weight: 500;
    position: relative;
    transition: var(--transition);
  }

  .nav-link:hover,
  .nav-link.active {
    color: var(--primary-blue);
  }

  .nav-link.active::after {
    content: '';
    position: absolute;
    bottom: -4px;
    left: 0;
    right: 0;
    height: 2px;
    background: var(--primary-blue);
    border-radius: 1px;
  }

  .nav-actions {
    display: flex;
    align-items: center;
    gap: var(--spacing-md);
  }

  .nav-lang {
    display: flex;
    align-items: center;
  }

  /* Language Switcher Styles */
  .language-switcher {
    position: relative;
    display: inline-block;
  }

  .lang-btn {
    display: flex;
    align-items: center;
    gap: var(--spacing-xs);
    padding: 0.5rem 0.75rem;
    background: transparent;
    border: 1px solid var(--neutral-300);
    border-radius: 6px;
    color: var(--neutral-600);
    cursor: pointer;
    transition: var(--transition);
    font-size: 0.875rem;
    font-weight: 500;
  }

  .lang-btn:hover {
    background: var(--neutral-50);
    border-color: var(--primary-blue);
    color: var(--primary-blue);
  }

  .lang-icon,
  .chevron {
    width: 16px;
    height: 16px;
  }

  .chevron {
    transition: var(--transition);
  }

  .lang-btn.active .chevron {
    transform: rotate(180deg);
  }

  .lang-btn-content {
    display: flex;
    align-items: center;
    gap: var(--spacing-xs);
  }

  .lang-dropdown {
    position: absolute;
    top: 100%;
    right: 0;
    margin-top: 0.25rem;
    background: white;
    border: 1px solid var(--neutral-200);
    border-radius: var(--border-radius);
    box-shadow: var(--shadow-lg);
    min-width: 180px;
    opacity: 0;
    visibility: hidden;
    transform: translateY(-10px);
    transition: var(--transition);
    z-index: 1000;
  }

  .lang-dropdown.active {
    opacity: 1;
    visibility: visible;
    transform: translateY(0);
  }

  .lang-option {
    display: flex;
    align-items: center;
    gap: var(--spacing-sm);
    width: 100%;
    padding: 0.75rem 1rem;
    background: none;
    border: none;
    text-align: left;
    cursor: pointer;
    transition: var(--transition);
    color: var(--neutral-700);
    font-size: 0.875rem;
  }

  .lang-option:hover {
    background: var(--neutral-50);
  }

  .lang-option:first-child {
    border-radius: var(--border-radius) var(--border-radius) 0 0;
  }

  .lang-option:last-child {
    border-radius: 0 0 var(--border-radius) var(--border-radius);
  }

  .flag {
    font-size: 1.125rem;
  }

  .mobile-lang-header {
    display: none;
  }

  .mobile-lang-btn {
    width: 100%;
    justify-content: space-between;
    background: var(--neutral-50);
    border-color: var(--neutral-200);
  }

  .mobile-lang-dropdown {
    position: static;
    opacity: 1;
    visibility: visible;
    transform: none;
    margin-top: var(--spacing-sm);
    box-shadow: none;
    border: 1px solid var(--neutral-200);
    display: none;
  }

  .mobile-lang-dropdown.active {
    display: block;
  }

  .mobile-menu-btn {
    display: none;
    flex-direction: column;
    background: none;
    border: none;
    cursor: pointer;
    padding: var(--spacing-xs);
    gap: 4px;
  }

  .mobile-menu-btn span {
    width: 24px;
    height: 2px;
    background: var(--neutral-800);
    transition: var(--transition);
  }

  .mobile-menu {
    display: none;
    position: fixed;
    top: 72px;
    left: 0;
    right: 0;
    background: white;
    border-top: 1px solid var(--neutral-200);
    box-shadow: var(--shadow-lg);
    transform: translateY(-100%);
    transition: transform 0.3s ease;
    z-index: 1000;
  }

  .mobile-menu.active {
    transform: translateY(0);
  }

  .mobile-menu-content {
    padding: var(--spacing-lg);
    display: flex;
    flex-direction: column;
    gap: var(--spacing-md);
  }

  .nav-content {
    gap: var(--spacing-sm);
    padding: 0 1rem;
  }

  .mobile-nav-link {
    text-decoration: none;
    color: var(--neutral-600);
    font-weight: 500;
    padding: var(--spacing-sm) 0;
    border-bottom: 1px solid var(--neutral-100);
    transition: var(--transition);
  }

  .mobile-nav-link:hover {
    color: var(--primary-blue);
  }

  .mobile-actions {
    margin-top: var(--spacing-sm);
  }

  @media (max-width: 768px) {
    .nav-links {
      display: none;
    }

    .nav-actions {
      display: none;
    }

    .mobile-lang-header {
      display: block;
    }

    .mobile-menu-btn {
      display: flex;
      align-items: center;
      justify-content: center;
      width: 36px;
      height: 36px;
      background: none;
      border: none;
      cursor: pointer;
      z-index: 1100;
      border-radius: 6px;
      transition: background-color 0.2s ease;
      flex-shrink: 0;
    }

    .mobile-menu-btn:hover {
      background: var(--neutral-100);
    }

    .mobile-menu-btn span {
      display: block;
      width: 20px;
      height: 2px;
      background: var(--neutral-800);
      border-radius: 2px;
      transition: all 0.3s ease;
      position: relative;
    }

    .mobile-menu-btn span:nth-child(1) {
      transform: translateY(-6px);
    }

    .mobile-menu-btn span:nth-child(3) {
      transform: translateY(6px);
    }

    /* Hamburger transformation when active */
    .mobile-menu-btn.active span:nth-child(1) {
      transform: translateY(0) rotate(45deg);
    }
    .mobile-menu-btn.active span:nth-child(2) {
      opacity: 0;
      transform: scale(0);
    }
    .mobile-menu-btn.active span:nth-child(3) {
      transform: translateY(0) rotate(-45deg);
    }

    .mobile-menu {
      display: block;
      position: fixed;
      top: 60px;
      left: 0;
      right: 0;
      background: white;
      border-top: 1px solid var(--neutral-200);
      box-shadow: 0 4px 20px rgba(0, 0, 0, 0.1);
      transform: translateY(-100%);
      transition: transform 0.3s cubic-bezier(0.4, 0, 0.2, 1);
      z-index: 999;
      max-height: calc(100vh - 60px);
      overflow-y: auto;
    }

    .mobile-menu.active {
      transform: translateY(0);
    }

    .mobile-menu-content {
      padding: 0;
      display: flex;
      flex-direction: column;
      gap: 0;
    }

    .mobile-nav-link {
      font-size: 1rem;
      font-weight: 500;
      padding: 1rem 1.5rem;
      border-bottom: 1px solid var(--neutral-100);
      color: var(--neutral-700);
      transition: color 0.2s ease;
      display: block;
    }

    .mobile-nav-link:hover {
      color: var(--primary-blue);
    }

    .mobile-nav-link:last-of-type {
      border-bottom: none;
    }

    .mobile-actions {
      margin: 0;
      padding: 1.5rem;
      border-top: 1px solid var(--neutral-100);
    }

    .mobile-actions .btn-primary {
      width: 100%;
      padding: 0.875rem 1rem;
      font-size: 1rem;
      font-weight: 600;
      text-align: center;
    }

    .mobile-lang-btn-header {
      display: flex;
      align-items: center;
      gap: 0.25rem;
      padding: 0.375rem 0.5rem;
      background: transparent;
      border: 1px solid var(--neutral-300);
      border-radius: 6px;
      color: var(--neutral-600);
      cursor: pointer;
      transition: var(--transition);
      font-size: 0.75rem;
      font-weight: 500;
      min-width: 70px;
      height: 36px;
    }

    .mobile-lang-btn-header:hover {
      background: var(--neutral-50);
      border-color: var(--primary-blue);
      color: var(--primary-blue);
    }

    .mobile-lang-btn-header .chevron {
      transform: rotate(0deg);
      transition: transform 0.3s ease;
      width: 12px;
      height: 12px;
    }

    .mobile-lang-btn-header.active .chevron {
      transform: rotate(180deg);
    }

    .mobile-lang-btn-header .lang-icon {
      width: 12px;
      height: 12px;
    }

    .mobile-lang-dropdown-header {
      position: absolute;
      top: 100%;
      right: 0;
      margin-top: 0.25rem;
      background: white;
      border: 1px solid var(--neutral-200);
      border-radius: 6px;
      box-shadow: var(--shadow-lg);
      min-width: 160px;
      opacity: 0;
      visibility: hidden;
      transform: translateY(-10px);
      transition: var(--transition);
      z-index: 1000;
    }
    
    .mobile-lang-dropdown-header.active {
      opacity: 1;
      visibility: visible;
      transform: translateY(0);
    }
    
    .brand-name {
      font-size: 1.125rem;
    }
    
    .logo-text .tagline {
      display: none;
    }

    /* Header height adjustment */
    .header {
      min-height: 60px;
    }

    .nav {
      padding: 0.5rem 0;
    }
  }

  
</style>

<script>
  import { getCurrentLanguage, setLanguage, getTranslation } from '../utils/i18n';

  document.addEventListener('DOMContentLoaded', () => {
    // Initialize language
    const currentLang = getCurrentLanguage();
    updateLanguageDisplay(currentLang);
    updatePageLanguage(currentLang);

    // Mobile menu functionality
    const mobileMenuBtn = document.getElementById('mobileMenuBtn');
    const mobileMenu = document.getElementById('mobileMenu');

    mobileMenuBtn?.addEventListener('click', () => {
      mobileMenu?.classList.toggle('active');
      mobileMenuBtn.classList.toggle('active');
    });

    // Close mobile menu when clicking on links
    const mobileNavLinks = document.querySelectorAll('.mobile-nav-link');
    mobileNavLinks.forEach(link => {
      link.addEventListener('click', () => {
        mobileMenu?.classList.remove('active');
        mobileMenuBtn?.classList.remove('active');
      });
    });

    // Header scroll effect
    let lastScroll = 0;
    const header = document.querySelector('.header');

    window.addEventListener('scroll', () => {
      const currentScroll = window.pageYOffset;

      if (currentScroll <= 0) {
        header?.classList.remove('scroll-up');
        return;
      }

      if (currentScroll > lastScroll && !header?.classList.contains('scroll-down')) {
        header?.classList.remove('scroll-up');
        header?.classList.add('scroll-down');
      } else if (currentScroll < lastScroll && header?.classList.contains('scroll-down')) {
        header?.classList.remove('scroll-down');
        header?.classList.add('scroll-up');
      }

      lastScroll = currentScroll;
    });

    // Language switcher functionality
    setupLanguageSwitcher('navLangBtn', 'navLangDropdown', 'navCurrentLang');
    setupLanguageSwitcher('mobileLangBtnHeader', 'mobileLangDropdownHeader', 'mobileCurrentLangHeader');

    // Listen for language changes
    window.addEventListener('languageChanged', (e) => {
      updatePageLanguage(e.detail);
    });
  });

  function setupLanguageSwitcher(btnId, dropdownId, currentLangId) {
    const langBtn = document.getElementById(btnId);
    const langDropdown = document.getElementById(dropdownId);
    const currentLangSpan = document.getElementById(currentLangId);

    if (!langBtn || !langDropdown) return;

    // Toggle dropdown
    langBtn.addEventListener('click', (e) => {
      e.stopPropagation();
      langBtn.classList.toggle('active');
      langDropdown.classList.toggle('active');
    });

    // Handle language selection
    const langOptions = langDropdown.querySelectorAll('.lang-option');
    langOptions.forEach(option => {
      option.addEventListener('click', (e) => {
        const selectedLang = e.currentTarget.dataset.lang;
        if (selectedLang) {
          setLanguage(selectedLang);
          updateLanguageDisplay(selectedLang);
          langBtn.classList.remove('active');
          langDropdown.classList.remove('active');
          
          // Close mobile menu if open
          const mobileMenu = document.getElementById('mobileMenu');
          const mobileMenuBtn = document.getElementById('mobileMenuBtn');
          mobileMenu?.classList.remove('active');
          mobileMenuBtn?.classList.remove('active');
          
          // Reload page to apply new language
          window.location.reload();
        }
      });
    });

    // Close dropdown when clicking outside
    document.addEventListener('click', () => {
      langBtn.classList.remove('active');
      langDropdown.classList.remove('active');
    });
  }

  function updateLanguageDisplay(lang) {
    const displays = ['navCurrentLang', 'mobileCurrentLangHeader'];
    displays.forEach(id => {
      const element = document.getElementById(id);
      if (element) {
        element.textContent = lang.toUpperCase();
      }
    });
  }

  function updatePageLanguage(lang) {
    // Update all elements with data-i18n attribute
    const elements = document.querySelectorAll('[data-i18n]');
    elements.forEach(element => {
      const key = element.getAttribute('data-i18n');
      if (key) {
        element.textContent = getTranslation(key, lang);
      }
    });
  }
</script>
